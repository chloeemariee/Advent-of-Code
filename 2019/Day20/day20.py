# Advent of Code 2019 Day 20
# Donut Maze

#planning

import math
import os
import pathlib
import sys

# biggest challenge will be to parse the maze

with open("input.txt") as f:
    for line in f:

#read the maze
def portals

def starting_point
    center to AA

def last_gate
    finding_ZZ

# play the game
def move
    current_position (starting_point)
    hunt_gates (unlocked)
    hunt_keys (stored_keys)
    where to go next

def hunt_keys
    if theres a key close by how far is it
    stored_keys

def hunt_gates
    if theres a key close by how far is it
    open_gates