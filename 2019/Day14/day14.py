# Advent of Code 2019 Day 14
# Space Stiochiometry
 
import sys, math

# file IO split into a list of formulas
formulas = [int(i) for i in open('input.txt', 'r').read().strip()]

#elements split by , = [ ] + [ ]
#transform ==> into =

line.split(' => ');

sort_formulas; 
    find."ORE"
    regular forumulas
    find."FUEL" 

def amounts 
    remember who you are

# Part two, count and quantify

def make_fuel (amounts, formulas) {
    start with ore
    what do you need to make fuel
}

print("how much ore for fuel", steps);